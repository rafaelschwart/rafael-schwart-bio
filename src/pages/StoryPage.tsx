import { Story } from "@/components/story/Story"

/**
 * Local design iteration — the long-form narrative version of the site.
 * Lives at /story, intentionally outside VALID_SECTIONS / sitemap / llms.txt.
 */
const StoryPage = () => <Story />

export default StoryPage
